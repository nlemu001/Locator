<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/RelativeLayout1"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical" >

    <EditText
        android:id="@+id/editText1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:ems="10"
        android:hint="New Circle Name"
        android:inputType="text" >
    </EditText>

    <ListView
        android:id="@+id/usersList"
        android:layout_width="match_parent"
        android:layout_height="334dp"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/create_circle_textView"
        android:choiceMode="multipleChoice" >
    </ListView>

    <Button
        android:id="@+id/create_circle_btn"
        android:layout_width="148dp"
        android:layout_height="38dp"
        android:layout_alignParentRight="true"
        android:layout_below="@+id/editText1"
        android:text="Create Circle" />

    <TextView
        android:id="@+id/create_circle_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/create_circle_btn"
        android:layout_alignParentLeft="true"
        android:layout_alignTop="@+id/create_circle_btn"
        android:layout_toLeftOf="@+id/create_circle_btn"
        android:text="Your Contacts:"
        android:textSize="20sp" />

</RelativeLayout>